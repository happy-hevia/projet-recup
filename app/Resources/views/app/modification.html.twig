
{% extends 'base.html.twig' %}

{% block body %}
<br />	
	{{ evenement.title }}
	
	{{ form_start(form) }}
	{{ form_row(form) }}
	{{ form_end(form) }}
	
	<button><a href="{{ path('app_app_evenement') }}">Annuler</a></button>
	
	{{ tinymce_init() }}
{% endblock body %}